"""
ElectGuide Backend — app package
=================================
This __init__.py marks the `app` directory as a Python package,
enabling imports like `from app.config import Settings` and
`from app.main import app`.

Without this file, Python will not recognise `app/` as a package
and all relative imports will fail.
"""
